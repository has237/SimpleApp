pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'docker build -t hasanie/SimpleApp .'
            }
        }
        
        stage('Deploy') {
            steps {
                sh 'docker push hasanie/SimpleApp'
            }
        }
    }
}